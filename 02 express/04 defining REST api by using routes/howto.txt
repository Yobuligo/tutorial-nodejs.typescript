1. create folder routes
2. create folder for model
    2.1. define the model
3. create a route for an entity, e.g. todos.ts
    3.1. define a router 
    3.2. define the routes itself
4. Call use(router) in app.ts
... implement the project as implemented :P

Important! To start the sever we still have to call "node app.js" but not "node app.ts", as Node.js only understands JavaScript